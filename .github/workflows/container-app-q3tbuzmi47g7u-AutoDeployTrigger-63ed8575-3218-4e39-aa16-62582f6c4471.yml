name: Trigger auto deployment for container-app-q3tbuzmi47g7u

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-q3tbuzmi47g7u-AutoDeployTrigger-63ed8575-3218-4e39-aa16-62582f6c4471.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPQ3TBUZMI47G7U_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPQ3TBUZMI47G7U_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPQ3TBUZMI47G7U_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPQ3TBUZMI47G7U_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPQ3TBUZMI47G7U_REGISTRY_PASSWORD }}
          containerAppName: container-app-q3tbuzmi47g7u
          resourceGroup: todonodejspgsqlgoogleauthb8908be2
          imageToBuild: default/container-app-q3tbuzmi47g7u:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


